# Frontend

React dashboard for:

- overview metrics
- trend graph (fake%, sarcasm%, avg trust)
- review explorer + import flows
- product trust table
- consumer trust table

## Run

```bash
npm install
npm run dev
```

By default, frontend requests `/api` and proxies to `http://localhost:4000`.
